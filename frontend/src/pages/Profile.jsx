import React from "react";
import { Box, Typography } from "@mui/material";

const Profile = () => {
  return (
    <Box sx={{ padding: 4 }}>
      <Typography variant="h4">Profile Page (Temp Page)</Typography>
      <Typography>This is your profile page.</Typography>
    </Box>
  );
};

export default Profile;
